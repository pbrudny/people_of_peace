<h1>Znaleziono pomoc!</h1>

<p>
  <%= link_to(@user.name, @user, target: '_blank') %> zaopiekuje się człowiekiem pokoju: <%= link_to(@person.name, @person, target: '_blank') %> (<%= @person.city %>)
</p>

<p>
  Dziękujemy!
</p>
  <hr />
  <h2>Pozostali ludzie pokoju bez opieki: </h2>

<ul>
  <% @people_free.each do |person| %>
    <p>
      <li>
        <strong>
          <%= link_to("#{person.name} (#{person.city})", person, target: '_blank') %>
        </strong>
      </li>
    </p>
    <%= render partial: 'needs', locals: { person: person } %>
    <br />
  <% end %>
</ul>
